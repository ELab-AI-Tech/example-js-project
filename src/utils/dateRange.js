/**
 * Stub: ask AI to implement this function.
 *
 * Demo target for Failure mode 3 (Built right, but it doesn't work).
 *
 * Bad prompt:
 *   "Implement a function that returns true if two date ranges overlap, and
 *    false otherwise."
 *
 * Good prompt: walk it through TDD step-by-step (see exercises markdown).
 *
 * @param {{ start: Date, end: Date }} a
 * @param {{ start: Date, end: Date }} b
 * @returns {boolean}
 */
export function dateRangesOverlap(a, b) {
  throw new Error("Not implemented — this is the failure-mode-3 demo target.");
}
